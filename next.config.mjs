/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com', 'blog.useartools.com.br', 'upload.wikimedia.org', 'images.adsttc.com', 'encrypted-tbn0.gstatic.com', 'www.destinofrance.com.br'], // Adicione os domínios necessários
    },
};

export default nextConfig;
