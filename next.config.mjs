/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'www.google.com', 'blog.useartools.com.br', 'upload.wikimedia.org', 'images.adsttc.com', 'encrypted-tbn0.gstatic.com', 'www.destinofrance.com.br', 'x.share-architects.com',], 
        unoptimized: true
    },
};

export default nextConfig;
